# 不外发名单样例。复制为 no_send.txt 再填写；no_send.txt 是个人文件，不进仓库。
# 调模型（advisor.py）时，名单里的主机及其子域不发送：既不作为被问的主机，也不出现在前后连接里。
# 一行一个域名，按后缀匹配（写 example.edu 即含全部子域）。# 之后为注释。没有 no_send.txt 时不过滤。
#
# 建议收录的几类（按「发出去会暴露什么」判断）：
# - 学校、单位的内部服务：暴露所在单位，内部主机名本身也不该外传
# - 代理服务商、订阅地址：暴露所用的代理
# - 浏览记录里最敏感的一类站点（及其广告网络）
# - 其它不想让模型服务商看到的主机

example.edu
example-vpn-provider.net
